# Published under the GitHub-verified Maven Central namespace. Release builds
# override the version with -Pversion=<tag> (see the release workflow).
group=io.github.thomaslaich.bote
version=0.1.0-SNAPSHOT
